class MainTitle {
  render = () => {
    const container = document.createElement('div');
    container.className = 'main-title';
    container.innerText = '';

    return container;
  };
}

export default MainTitle;
